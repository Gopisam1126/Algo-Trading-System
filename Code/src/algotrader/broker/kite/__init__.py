"""Zerodha Kite Connect implementation of the broker protocols.

Nothing outside this package imports ``kiteconnect``. That is the whole point
of the split: swapping brokers touches this directory and nothing else.
"""
